[pytest]
addopts = --basetemp=.tmp-pytest-local -p no:cacheprovider
testpaths = tests
norecursedirs = archive state .tmp-pytest-local pytest-cache-files-*
markers =
    integration: opt-in real-runtime Fun Time integration tests that launch the app on Windows
