archive/
state/

favs.csv

# Claude Code local config
.claude/

.venv/
__pycache__/
**/__pycache__/
*.py[cod]
.coverage
.pytest_cache/
.hypothesis/
.tmp-pytest-*/
pytest-cache-files-*/

# Sanitization overlay — the real blocklist is a catalogue of excluded terms, so
# it is never committed. Copy sanitize/blocklist.example.txt to fill it in.
/sanitize/blocklist.local.txt
